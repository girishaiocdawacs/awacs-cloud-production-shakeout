name: Stop SQL
on:
  schedule:
    - cron:  '30 16 * * *' 
jobs:
  stop:
    name: Stop Instance Nightly
    runs-on: ubuntu-latest
    steps:
    - name: Setup
    - uses: google-github-actions/setup-gcloud@master
      with:
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ secrets.GKE_PROJECT }}
    - run: |-
        gcloud sql instances patch awacs-test-db --activation-policy NEVER
        
